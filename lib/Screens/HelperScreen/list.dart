import 'images.dart';

const ListSlider = [avenger, cartoon, strange,];
// onPageChanged: (index, _) {
// setState(() {
// _currentIndex = index;
// });
// },
// itemBuilder: (context, index) {
// return Image.asset(
// ListSlider[index],
// fit: BoxFit.fitWidth,
// );
// }